---
# Base install for all systems

- include: install.yml
- include: java.yml
- include: users.yml

- name: Configuring logrotate
  copy: src=iptables.logrotate dest=/etc/logrotate.d/iptables

- name: Configuring sshd
  file: src=sshd_config dest=/etc/ssh/sshd_config
  tags: sshd
  notify: restart sshd
